FROM golang:1.26-alpine AS build
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/hub-server ./cmd/hub-server

FROM alpine:3.20
COPY --from=build /out/hub-server /usr/local/bin/hub-server
ENTRYPOINT ["hub-server"]
